class TopLayerSequence(Layer):
    layers = []

    layersIter = iter(layers)

    layer

    logger

    def __init__(self, layers):
        self.layers = layers

    def isTaskDone(self):
        # not sure how to get hasNext() for iterator.
        return layer.isTaskDone()

    def setup(self, setupInfo):
        #placeholder
        pass